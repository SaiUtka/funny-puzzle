import {Cards, ChangeText, Modal} from '../index';

function Main() {

    return (
        <main className={'main'}>
            <ChangeText/>
            <Cards/>
            <Modal/>
        </main>
    );
}

export {Main};